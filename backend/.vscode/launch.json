{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Python: FastAPI",
        "type": "debugpy",
        "request": "launch",
        "program": "${workspaceFolder}/src/main.py",
        "envFile": "${workspaceFolder}/src/.env",
        "cwd": "${workspaceFolder}/src"
      }
    ]
  }